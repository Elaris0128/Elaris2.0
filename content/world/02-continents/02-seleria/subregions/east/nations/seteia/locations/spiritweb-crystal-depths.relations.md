# 靈網晶層人物關係

- $(@{Name=晶聽; Desc=節點維護者，對微小波動極敏感}.Name) ↔ 深芽：兩人都在 靈網晶層 的 晶脈橋 一帶有話語權，但對 $((靈網晶層的規矩不是禮貌問題，而是整個國家能不能穩定的問題 最近有幾個外層節點出現像被遠方輕敲過的異常回振[0 % 靈網晶層的規矩不是禮貌問題，而是整個國家能不能穩定的問題 最近有幾個外層節點出現像被遠方輕敲過的異常回振.Count] -replace '[。！？]','').Trim()) 的處理方式不完全一樣。
- $(@{Name=深芽; Desc=地底引導者，只在必要時出聲提醒}.Name) ↔ 回枝：兩人都在 靈網晶層 的 共鳴井 一帶有話語權，但對 $((靈網晶層的規矩不是禮貌問題，而是整個國家能不能穩定的問題 最近有幾個外層節點出現像被遠方輕敲過的異常回振[1 % 靈網晶層的規矩不是禮貌問題，而是整個國家能不能穩定的問題 最近有幾個外層節點出現像被遠方輕敲過的異常回振.Count] -replace '[。！？]','').Trim()) 的處理方式不完全一樣。
- $(@{Name=回枝; Desc=封鎖操作棲裔，負責危機時切斷異常節點}.Name) ↔ 晶聽：兩人都在 靈網晶層 的 節點室 一帶有話語權，但對 $((靈網晶層的規矩不是禮貌問題，而是整個國家能不能穩定的問題 最近有幾個外層節點出現像被遠方輕敲過的異常回振[2 % 靈網晶層的規矩不是禮貌問題，而是整個國家能不能穩定的問題 最近有幾個外層節點出現像被遠方輕敲過的異常回振.Count] -replace '[。！？]','').Trim()) 的處理方式不完全一樣。
- 地方人物 ↔ 維護架：很多看似零碎的消息都會先在這裡變成能被交換的情報。
